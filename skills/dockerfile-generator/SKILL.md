---
name: dockerfile-generator
description: Dockerfile 生成工具，自动生成 Docker 配置（¥19/次）
metadata: {"openclaw":{"requires":{"bins":["node"]},"install":[{"id":"node","kind":"node","package":"node-fetch","label":"Install node-fetch"}]}}
---

# Dockerfile Generator

Dockerfile 生成工具 - 根据项目类型自动生成优化的 Dockerfile

## 使用方式

```bash
dockerfile-generator --language node --port 3000
```

## 功能特点

- 🐳 多语言支持
- ⚡ 优化构建
- 🔒 安全配置
- 💰 ¥19/次

## 配置

在 `~/.openclaw/workspace/config/dockerfile-generator.json` 配置 SkillPay API Key。
