#!/usr/bin/env node
const fs=require('fs'),path=require('path'),CONFIG_PATH=path.join(process.env.HOME,'.openclaw/workspace/config/markdown-editor.json'),loadConfig=()=>JSON.parse(fs.readFileSync(CONFIG_PATH,'utf8'));
async function chargeUser(userId,amount){const config=loadConfig(),fetch=require('node-fetch');for(const e of['https://api.skillpay.me/billing/charge','https://skillpay.me/api/billing/charge']){try{const r=await fetch(e,{method:'POST',headers:{'Authorization':`Bearer ${config.skillpay_api_key}`,'Content-Type':'application/json'},body:JSON.stringify({user_id:userId,skill_id:'markdown-editor',amount,currency:'CNY'}),timeout:5000});return await r.json()}catch(e){continue}}return{success:false,payment_url:'https://skillpay.me/topup'}}
function convertMD(text){const html=text.replace(/^# (.*$)/gim,'<h1>$1</h1>').replace(/^## (.*$)/gim,'<h2>$1</h2>').replace(/\*\*(.*)\*\*/gim,'<b>$1</b>').replace(/\*(.*)\*/gim,'<i>$1</i>').replace(/\[(.*)\]\((.*)\)/gim,'<a href="$2">$1</a>');return{success:true,markdown:text,html}}
async function main(){const args=process.argv.slice(2),config=loadConfig();if(!args[0]){console.log('用法：markdown-editor "<Markdown>"');return}const text=args[0],price=config.price_per_call||0.5,userId=process.env.USER||'unknown';console.log(`📝 Markdown Editor\n💰 费用：¥${price}\n`);const chargeResult=await chargeUser(userId,price);if(!chargeResult.success){console.error('❌ 收费失败');console.log(`💳 ${chargeResult.payment_url}`);process.exit(1)}console.log('✅ 收费成功\n📝 正在转换...');const result=convertMD(text);console.log('\n━━━ HTML 结果 ━━━\n'+result.html+'\n━━━ 结束 ━━━')}main().catch(e=>{console.error('❌',e.message);process.exit(1)});
