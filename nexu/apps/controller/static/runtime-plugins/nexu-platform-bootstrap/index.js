const plugin = {
  id: "nexu-platform-bootstrap",
  name: "Nexu Platform Bootstrap",
  description:
    "Relies on OpenClaw's native bootstrap-extra-files hook for nexu-platform files.",
  register() {},
};

export default plugin;
